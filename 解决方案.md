# dsh-files-panel —— 完整解决方案(给使用者看)

> 本文件汇总了这次开发的所有成果:修复的 bug、交付的三项功能、如何使用、以及 GitHub 同步情况。
> 看完本文件即可上手。更详细的中英文安装/使用步骤见 `docs/USAGE.md`(英文)和 `docs/USAGE.zh.md`(中文)。

---

## 一、刚修复的 Bug(重要)

**现象**:分屏功能上线后,页面变乱——输入框跑到了最上面,显示对话消息的区域消失了;而且**硬刷新、重启服务都不管用**。

**真正原因(已定位并修复)**:浏览器 localStorage 里存着分屏上线**之前**的旧界面状态。旧状态里只有 `view` 一个字段;而新代码需要 `panes`(分屏树)字段。程序读 localStorage 时是「整份替换」,不会补上新字段——于是 `panes` 为 `undefined`,分屏组件直接崩溃,只剩输入框还留在页面上。崩溃后错误被界面兜底层接住、只显示一个图标,所以看不到报错,看起来就像「布局坏了」。

**修复**:把持久化存储的 key 从 `dsh.conversation.chat` 改成 `dsh.conversation.chat.v2`。这样旧的那份不兼容数据会失效被丢弃,新代码用全新 key 重新写入正确结构(代价只是:升级这一次会丢掉之前没发出去的那条草稿/选区)。

**你现在要做的**:
1. 已重新构建 bundle(已验证产物里是新 key `dsh.conversation.chat.v2`)。
2. 硬刷新 `Ctrl+F5`。若还不行,打开浏览器开发者工具(F12)→ Console 执行下面这行,清掉旧的坏状态再刷新:
   ```js
   localStorage.removeItem('dsh.conversation.chat')
   ```
   (旧的 key 名就是 `dsh.conversation.chat`,新 key 是 `.v2`,不会误删新数据。)
3. 服务本身不需要重启(bundle 从磁盘按请求读取);若确实想重启:

```powershell
cd D:\projects\deepseek-harness\deepseek-harness
pnpm dsh --profile web
```

---

## 二、交付的三项功能(都在 `dsh web` 页面里)

### 1. Terminal 标签页(终端)
- 打开会话后,顶部标签栏点 **「终端 / Terminal」**(在 对话/Chat、Trajectory 旁边)。
- 直接输入 shell 命令。Windows 上是 PowerShell,Linux/macOS 上是 bash。
- 切走再切回会保留同一个 shell(自动重挂载);「重启终端」按钮重开一个新 shell。

### 2. 文件资源管理器
- 点会话头部右上角的 **文件夹图标**。
- 右侧弹出当前工作区的**文件树**;每行有**类型小图标**(代码 / json / markdown / 图片 / 配置 等)。
- 点文件打开,带**语法高亮**;可直接编辑,**保存 / Save**(或 Ctrl/Cmd+S),有冲突时提示重新载入;键位可切换 默认 / Vim / Emacs。

### 3. tmux 式分屏
- 每个窗格顶部有一条细工具栏,四个按钮:
  - **⫞** 向右分割(并排)
  - **⫟** 向下分割(堆叠)
  - **⛶** 全屏此窗格(再点退出)
  - **✕** 关闭此窗格
- 分割会新建一个窗格(默认 Chat)。切换某窗格的视图:先点它(聚焦),再点顶部标签(Chat / Trajectory / Terminal)。
- 拖动窗格之间的分隔条调整大小(每边至少保留 10%)。
- 分割可**递归**,布局按会话持久化。

---

## 三、如何安装 / 运行这个插件(给别人用)

插件不是独立 npm 包,而是一组在 `deepseek-harness` 单仓库里的包,以「累积 git 补丁」形式分发。

1. 准备一份 deepseek-harness checkout,checkout 到 base commit `47f943859b`。
2. 在仓库根执行(用**最新**补丁 `c10`,已含全部):
   ```bash
   git apply ../dsh-files-panel/patch/c10-persist-key-fix.dsh.diff
   pnpm install
   pnpm run build
   ```
3. 启动(注意**不要**用 `npx`):
   ```bash
   pnpm dsh --profile web
   ```
4. 浏览器打开打印出的 URL(通常 `http://127.0.0.1:3080`),硬刷新 `Ctrl+F5`。

> 强调:必须用 `pnpm dsh --profile web` 从工作区启动,不能用 `npx @deepseek-ai/dsh web`——后者服务的是 npm 缓存里的已发布快照,不含本插件改动。

---

## 四、GitHub 同步情况

代码已按里程碑(检查点)逐步推送到私有仓库 `github.com/JiayuuWang/dsh-files-panel`(最新提交 `8809036`):

| 检查点 | 内容 |
|---|---|
| C1–C5 | 文件面板基础(files 能力缝 + 面板 + 编辑保存 + vim/emacs 键位 + 详情入口) |
| C6 | Terminal 标签页 |
| C7 | Explorer 开关 + 文件图标 + 语法高亮 |
| C8 | tmux 式分屏 |
| C9 | 分屏布局(overlay/滚动模型)修复 |
| C10 | **持久化状态修复(真正的根因,本次)** |

每个补丁都是累积的(从 base commit 起),只需要应用最新一个 `c10`。

---

## 五、已知限制

- 终端是轮询式输出(约 60ms)而非流式;尺寸固定,并继承 `TERM=dumb`(暂无彩色/全屏程序)。
- 每会话一个终端。
- 同一视图的多个分屏会共享该视图状态(例如两个 Chat 窗格共享草稿/选中)。
- 文件树与终端只面向人,它们的内容不会注入给模型。

---

## 六、本次改动涉及的文件(工作区 `D:\projects\deepseek-harness\deepseek-harness`)

**Bug 修复(本次 C10,真正的根因)**:
- `packages/client/ui-conversation/src/client/stores.ts`(持久化 key `dsh.conversation.chat` → `dsh.conversation.chat.v2`)
- `packages/client/ui-conversation/tests/chat-store.client.spec.ts`、`selection-survival.client.spec.tsx`(同步 key 断言)

**Bug 修复(C9,布局层面的配套修复,仍保留)**:
- `packages/client/ui-conversation/src/client/PaneTree.tsx`(加 overlay / pane-view 标记)
- `packages/client/ui-conversation/src/client/PaneTree.module.css`(视图区改为 flex 列)
- `packages/client/ui-conversation/src/client/chat/ChatView.tsx`(窗格内自持滚动)
- `packages/client/ui-conversation/src/client/chat/ChatView.module.css`(窗格内滚动 + 底部留白)

**三项功能的核心包**:
- 终端:`packages/host/web-terminal`、`packages/host/web-terminal-local`、`packages/client/ui-terminal`
- 文件面板/资源管理器:`packages/client/ui-files`(图标 `FileIcon.tsx`、高亮 `EditorPane.tsx`)
- 分屏:`packages/client/ui-conversation`(`PaneTree.tsx`、`pane-tree.ts`、`contract/panes.ts`)

---

## 七、安全提醒

之前你把 GitHub token(`ghp_…`)贴在对话里了。建议尽快到 GitHub **Settings → Developer settings → Personal access tokens** 撤销该 token 并换一个新的,避免泄露。
